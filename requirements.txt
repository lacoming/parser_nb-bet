# parser_nb-bet dependencies
# Обновляй при добавлении новых библиотек

# HTTP client (async-capable, proxy support)
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Excel
openpyxl>=3.1.0

# Tray icon (system tray for Windows)
pystray>=0.19.0
Pillow>=10.0.0

# Scheduler with timezone support
APScheduler>=3.10.0
pytz>=2024.1

# Retry/backoff
tenacity>=8.2.0

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0
