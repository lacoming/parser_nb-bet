# parser_nb-bet Python dependencies

# HTTP client
requests>=2.31.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Fuzzy matching (team names)
rapidfuzz>=3.5.0

# Excel read + write (leagues.xlsx + results)
openpyxl>=3.1.0

# Dev / testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Packaging (install separately, not needed at runtime)
# pyinstaller>=6.0.0
